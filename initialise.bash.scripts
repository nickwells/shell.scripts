# -*- shell-script -*-

echo "initialising..."

chmod ugo+x bundleFiles makeQScript shout.alert shout.header shout.short shout.smallHeader

if type semverincr ; then
    chmod ugo+x semVerLatest semVerList semVerNext semVerUpdGoModToLatest
else
    echo you need the semver tools installed in order to use the semVer... scripts
fi
echo done
